services:
  postgres:
    image: postgres:16
    container_name: ragmax-postgres
    environment:
      POSTGRES_DB: ragmax
      POSTGRES_USER: ragmax
      POSTGRES_PASSWORD: ragmax
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

